export const enum ROLES {
    ADMIN = "ADMIN",
    CLIENT = "CLIENT",
}

export const enum MODEL_TYPE {
    CLASSIFY = "CLASSIFY",
    ANALYST = "ANALYST",
}

export const enum time {
    MILLISECONDS = "milliseconds",
    SECOND = "second",
    MINUTE = "minute",
    HOUR = "hour",
    DAY = "day",
}

export const enum CHEMICAL_TYPE {
    FAT = "FAT",
    PROT = "PROT",
    LACT = "LACT",
    SCC = "SCC",
    UREA = "UREA",
}

export const enum datetimeFormat {
    YYYY_MM_DD,
    YYYY_MM_DD__HH_MM,
}
